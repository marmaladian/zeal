class TileEditor:

    def __init__(self, tile_size = 8) -> None:
        self.current_tile = None

    def draw(self, surface, tile_pos = (0, 0)):
        pass
        # draw tilemap
        # draw some bonus stuff

    # write to tile
        # convert tilestring to pixels or bits
        # write to tile
        # tile can update its image
        # so tile has bitstring, then an image rep.

    # how to listen for events?


